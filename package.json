{
  "name": "lecturer-student-admin-cloudflare",
  "version": "2.2.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply lecturer-student-admin-db --local",
    "db:remote": "wrangler d1 migrations apply lecturer-student-admin-db --remote",
    "test": "node --check src/worker.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "^4.31.0"
  }
}
